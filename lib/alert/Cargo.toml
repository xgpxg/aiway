[package]
name = "alert"
version = "0.1.0"
edition = "2024"

[dependencies]
protocol = { path = "../protocol", features = ["alert"] }
reqwest = "0.12"
tokio = { version = "1", features = ["macros"] }
