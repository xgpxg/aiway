[package]
name = "aiway-model-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
derive_builder = "0.20"
bytes = { version = "1", features = ["serde"] }

[features]
stream = []