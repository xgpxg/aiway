[package]
name = "aiway"
version = "0.3.3"
edition = "2024"

[dependencies]
logging = { path = "../lib/logging" }
anyhow = "1"
tokio = { version = "1", features = ["macros"] }
rust-embed = "8"
clap = { version = "4.5", features = ["derive"] }
