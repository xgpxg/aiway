# 接入层

接入层（Access）是网关系统的 L4 TCP 透传代理，支持 TLS 终止。作为外部流量的统一入口，将请求转发到后端网关节点。

## 职责

- **统一入口**：作为系统的对外统一接入点
- **L4 透传**：纯 TCP 层转发，不解析 HTTP 内容
- **TLS 终止**：解密 HTTPS 流量后以明文转发给网关
- **SNI 路由**：根据域名动态选择 TLS 证书
- **节点发现**：从控制台自动发现网关节点
- **负载均衡**：轮询分发到多个网关节点
