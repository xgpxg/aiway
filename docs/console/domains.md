# 域名管理

管理域名证书，配置 TLS 终止。

## 功能

- **域名绑定**：将域名与服务关联
- **证书管理**：上传 SSL/TLS 证书
- **自动匹配**：SNI 自动选择对应证书
- **通配符支持**：`*.example.com` 匹配所有子域名

## 证书格式

支持 PEM 格式的证书文件和私钥文件。支持证书链（含中间 CA 证书）。

## 同步

证书上传后自动同步到 Access 层，30 秒内生效，无需重启。
